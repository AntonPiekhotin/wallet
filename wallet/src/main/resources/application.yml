server:
  port: 0

spring:
  application:
    name: wallet
  datasource:
    url: jdbc:postgresql://localhost:5435/wallet
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
